app_name: ${process.env.npm_package_name || "my-application"}
environment: production
framework: nodejs
region: us-west-2
provider: aws
resources:
  memory: 512
  cpu: 0.5
environment_variables:
  NODE_ENV: production
  DEBUG: false 